---
applications:
- name: closed-sales
  random-route: true
  memory: 256M
  instances: 1
  path: .
  command: bundle exec rails db:migrate && rails db:seed && bundle exec rails s -p $PORT
  services:
    - closed-sales-postgres